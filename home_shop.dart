import 'package:flutter/material.dart';

class HomeShopPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Shopper Home'),
      ),
      body: Center(
        child: Text('Welcome Shopper!'),
      ),
    );
  }
}
